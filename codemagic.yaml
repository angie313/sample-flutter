# Automatically generated on 2022-06-15 UTC from https://codemagic.io/app/62a737b82b104074744365db/settings
# Note that this configuration is not an exact match to UI settings. Review and adjust as necessary.

workflows:
  default-workflow:
    name: Default Workflow
    max_build_duration: 60
    environment:
      flutter: default
      xcode: latest
      cocoapods: default
    scripts:
      - |
        # set up debug keystore
        rm -f ~/.android/debug.keystore
        keytool -genkeypair \
          -alias androiddebugkey \
          -keypass android \
          -keystore ~/.android/debug.keystore \
          -storepass android \
          -dname 'CN=Android Debug,O=Android,C=US' \
          -keyalg 'RSA' \
          -keysize 2048 \
          -validity 10000
      - |
        # set up local properties
        echo "flutter.sdk=$HOME/programs/flutter" > "$FCI_BUILD_DIR/android/local.properties"
      - flutter packages pub get
      - flutter build apk --debug
      - |
        # run testproject testcases
        curl -v -X POST "https://api.testproject.io/v2/projects/alTr088zEUWEliG3pBYi9w/tests/JXz8r8Dxqki7X3qVBVEN_w/run" -H "accept: application/json" -H "Authorization: n-aLvFCqZegwP7yUlzhVGQynSgYane3KrbwUln9Aiag1" -H "Content-Type: application/json" -d "{ \"agentId\": \"trNH_Sh_Uka4jvSVWR1z_A\", \"device\": \"bd102da7\", \"reinstallApp\": \"app/build/outputs/apk/debug/app-debug.apk\"}"
    artifacts:
      - build/**/outputs/apk/**/*.apk
      - build/**/outputs/bundle/**/*.aab
      - build/**/outputs/**/mapping.txt
      - '*.snap'
      - build/windows/**/*.msix
      - flutter_drive.log
    publishing:
      email:
        recipients:
          - nguan@centegix.com
